dsv#include<bits/stdc++.h> 
using namespace std ; 
typedef long long ll ; 
const ll N = 1e5 + 1 ; 

int main() 
{
	ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0)  ; 
	 int t ; 
	 cin >> t ; 
	 while( t-- ) 
	 {
	 	string s ; 
	 	cin >> s ; 
	 	ll n = s.length() ; 
	 	if ( n - 3  >= 0 and s.susbtr ( n - 3 , 2 ) == "po") 
	 		cout << "FILIPINO" << endl ; 
	 	else if ( n - 5 >= 0 and (  s.substr ( n - 5 , 4 ) == "desu" or s.substr ( n - 5 , 4 ) == "masu") )
	 		cout << "JAPANESE"" << endl; 
	 	else if ( n - 6 >= 0 and  s.substr ( n - 6 , 6 ) == "mnida" )
	 		cout << "KOREAN" << endl;    
	 }
	

}


